{% comment %}
  @param price {number}
  @param compare_at_price {number}
{% endcomment %}

{% assign current_price = price | default: 0 | plus: 0 %}
{% assign compare_price = compare_at_price | default: 0 | plus: 0 %}

{% if compare_price > current_price %}
  <span class="so-price so-price--sale">
    <span class="so-price__current">{{ current_price | money }}</span>
    <s class="so-price__compare">{{ compare_price | money }}</s>
  </span>
{% else %}
  <span class="so-price">{{ current_price | money }}</span>
{% endif %}
